import React from 'react';
import { Compiler } from './Components/Compiler';

const App: React.FC = () => {
    return (
        <div className="App">
            <Compiler />
        </div>
    );
};

export default App;
